# Publish the static HTML site (index.html, cv.html, etc.) to GitHub Pages.
# The old MkDocs workflow was overwriting gh-pages with docs/*.md output.
name: Deploy static site
on:
  push:
    branches:
      - main
permissions:
  contents: write
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Publish site root to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: .
          exclude_assets: |
            .github
            docs
            mkdocs.yml
            Fraunces
            Geist
            scripts
